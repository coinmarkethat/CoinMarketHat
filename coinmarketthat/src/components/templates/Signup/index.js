import React from 'react';

import { Signup } from '../../organisms';

const SignupTemplate = ({ theme, history }) => {
  return <Signup theme={theme} history={history} />;
};

export default SignupTemplate;
