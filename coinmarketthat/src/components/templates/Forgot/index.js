import React from 'react';

import { Forgot } from '../../organisms';

const ForgotTemplate = ({ theme, history }) => {
  return (
    <Forgot theme={theme} history={history} />
  );
};

export default ForgotTemplate;
