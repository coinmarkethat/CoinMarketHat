import React from 'react';

import { Login } from '../../organisms';

const LoginTemplate = ({ theme, history }) => {
  return (
    <Login theme={theme} history={history} />
  );
};

export default LoginTemplate;
