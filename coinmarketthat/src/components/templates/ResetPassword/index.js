import React from 'react';

import { Reset } from '../../organisms';

const ResetTemplate = ({ theme, history }) => {
  return (
    <Reset theme={theme} history={history} />
  );
};

export default ResetTemplate;
